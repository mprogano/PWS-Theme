{% include 'breadcrumb-page' %}
<div class="container">
    <div class="row">
      {% for collection in collections %}
        <div class="col-sp-12 col-xs-6 col-sm-6 col-md-3">
          {% include 'collection-grid-item' %}
        </div>
      {% endfor %}
    </div>
</div>

